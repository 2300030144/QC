version: '3.8'
services:
  travel_backend:
    # Builds the service using the existing Dockerfile in the current directory (.).
    build: .
    # Maps the container's port 8080 to your computer's port 8080.
    ports:
      - "8088:8080"
    # Restart the container if it stops, unless manually stopped.
    restart: always